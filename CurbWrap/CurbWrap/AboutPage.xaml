<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:img="clr-namespace:CurbWrap.MarkupExtensions;assembly=CurbWrap"
             x:Class="CurbWrap.AboutPage"
             Style="{StaticResource appPage}"
             Title="About CurbWrap">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="10">
                <Label Text="CurbWrap"
                       Style="{StaticResource Title}"
                       x:Name="Header"
                    VerticalOptions="Start" 
                    HorizontalOptions="CenterAndExpand" />
                <Image Aspect="AspectFit" 
                       Source="{img:EmbeddedImage ResourceName=CurbWrap.Images.Applogo.png}" 
                       HeightRequest="150"
                       WidthRequest="150"
                       Opacity="0.4" />
                <Label  LineBreakMode="WordWrap" MaxLines="40" x:Name="AboutText" Style="{StaticResource Label}" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>